import org.junit.Test;
import static org.junit.Assert.assertTrue;

public class ExampleTest {
    @Test
    public void testPipelineWorks() {
        assertTrue("This test always passes to prove the CI pipeline works", true);
    }
}
